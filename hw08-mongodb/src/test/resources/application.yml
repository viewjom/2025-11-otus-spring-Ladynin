spring:
  data:
    mongodb:
      port: 0
      database: main

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  runner-type: applicationrunner
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
  test-enabled: true