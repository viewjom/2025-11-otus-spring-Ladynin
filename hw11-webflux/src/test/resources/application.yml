spring:
  data:
    mongodb:
      port: 27017
      database: main
      host: localhost
      uri: mongodb://localhost:27017
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
  test-enabled: true